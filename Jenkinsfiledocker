pipeline {
    agent any
    tools {
        maven "Maven-3.9.15"
    }
    triggers{
        githubPush()
    }
    stages {
        stage('mvn build') {
            steps {
                // Get some code from a GitHub repository 
                mvn 'clean package'
            }

        }
    }
}
